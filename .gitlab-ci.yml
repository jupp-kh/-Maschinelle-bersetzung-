# Environment variable readme
# https://docs.gitlab.com/ee/ci/variables/

# Predefined environment variables
# https://docs.gitlab.com/ee/ci/variables/predefined_variables.html

# Using docker images
# https://docs.gitlab.com/ee/ci/docker/using_docker_images.html

# .gitlab-ci.yml reference
# https://docs.gitlab.com/ee/ci/yaml/

# Container registry reference (authenticating, etc.)
# https://gitlab.com/help/user/packages/container_registry/index

stages:
  - build
  - test
  - deploy



# You can define an image that’s used for all jobs, and a list 
# of services that you want to use during runtime.
default:
  image: tmaier/docker-compose

  services:
    - docker:dind

  before_script:
    - docker-compose build --no-cache

build-job:
  stage: build 
  script:
    - echo "I am useless"

test-job1:
  stage: test
  script:
    - docker-compose run ci-series python -m unittest discover -s ./tests

test-job2:
  stage: test
  script:
      - ./compile-to-discord.sh "$CI_COMMIT_SHA" "$DISCORD_WEBHOOK"
    artifacts:
      paths:
        - out


